{
  "name": "messenger-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npm run build && node server.cjs",
    "start vite": "vite build && node server.cjs"
  },
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.86.2",
    "vite": "^6.3.1"
  },
  "dependencies": {
    "express": "^5.1.0",
    "handlebars": "^4.7.8"
  }
}
